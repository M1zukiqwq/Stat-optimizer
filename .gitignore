.aris/
.claude/
CLAUDE.md
research-wiki/
venv/
.venv_v3/
attention_viz/

# LaTeX build artifacts
*.aux
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.blg
*.bbl
*.toc
*.lof
*.lot
*.bcf
*.run.xml

# Python
__pycache__/
*.pyc
*.pyo

# Large generated experiment row-level outputs
experiments/results/**/predicate_rows.*
experiments/results/**/decision_rows.*
experiments/results/**/composition_family_results.json
experiments/results/**/case_rows.*
experiments/results/**/plan_rows.*
experiments/results/**/batch_plan_rows.*

# Downloaded public trace caches
experiments/data/public_traces/*.gz
experiments/data/public_traces/*.tmp

# Archived exploratory experiment outputs (kept locally, never versioned)
experiments/_archive/

# Raw datasets (fetched via download_data.sh, never versioned)
experiments/data/

# Trained model checkpoints (binary artifacts, regenerated by train.py)
experiments/oasis_torch/artifacts/*.pt

# Third-party JOB clone (cloned from gregrahn/join-order-benchmark)
experiments/job_imdb/join-order-benchmark/

# OSS reproduction package (maintained as a separate GitHub repo)
oasis-artifact/

# Pre-rewrite paper backups
paper/*.bak_prerewrite_*
